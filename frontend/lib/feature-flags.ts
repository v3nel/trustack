/**
 * DevCycle Feature Flag Keys
 *
 * Define all feature flag variable keys as constants so they can be
 * referenced consistently across server and client code.
 *
 * Create matching variables in your DevCycle dashboard with the same
 * keys and a Boolean type (default = true to keep features enabled).
 */

/** Controls visibility of the Escrow / Payments feature */
export const FEATURE_ESCROW_ENABLED = "escrow-enabled";
